#include <iostream>
using namespace std;

int pairSum(int *arr, int n) {
	// Write your code here
}

int main() {
    int n;
    cin >> n;

    int* arr = new int[n];

    for (int i = 0; i < n; ++i) {
        cin >> arr[i];
    }

    cout << pairSum(arr, n);

    delete[] arr;
}